import { createTheme } from "@mui/material";

// Style

export const theme = createTheme({
  pallet: {
    primary: {
      main: "#1760a5",
      light: "skyblue",
    },
    secondary: {
      main: "#15c630",
    },
    otherColor: {
      main: "blue",
    },
  },
});

export default theme;
